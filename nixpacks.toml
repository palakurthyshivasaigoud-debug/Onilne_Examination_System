[phases.setup]
nixPkgs = [
  "python312",
  "libGL",
  "libglib",
  "zlib",
  "gcc"
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
